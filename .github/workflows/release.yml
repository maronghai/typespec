name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Zig
        uses: mlugg/setup-zig@v1
        with:
          version: 0.16.0

      - name: Build linux-x86_64
        run: cd zig-typespec && zig build -Doptimize=ReleaseSafe -Dtarget=x86_64-linux

      - name: Build linux-aarch64
        run: cd zig-typespec && zig build -Doptimize=ReleaseSafe -Dtarget=aarch64-linux

      - name: Build macos-x86_64
        run: cd zig-typespec && zig build -Doptimize=ReleaseSafe -Dtarget=x86_64-macos

      - name: Build macos-aarch64
        run: cd zig-typespec && zig build -Doptimize=ReleaseSafe -Dtarget=aarch64-macos

      - name: Build windows-x86_64
        run: cd zig-typespec && zig build -Doptimize=ReleaseSafe -Dtarget=x86_64-windows

      - name: Package binaries
        run: |
          cd zig-typespec/zig-out/bin
          tar czf ../../../typespec-linux-x86_64.tar.gz typespec
          tar czf ../../../typespec-linux-aarch64.tar.gz typespec
          tar czf ../../../typespec-macos-x86_64.tar.gz typespec
          tar czf ../../../typespec-macos-aarch64.tar.gz typespec
          7z a ../../../typespec-windows-x86_64.zip typespec.exe

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            typespec-linux-x86_64.tar.gz
            typespec-linux-aarch64.tar.gz
            typespec-macos-x86_64.tar.gz
            typespec-macos-aarch64.tar.gz
            typespec-windows-x86_64.zip
